[project]
name = "bindicator"
version = "0.1.0"
description = "Bin collection indicator — server for ESP32 client"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "beautifulsoup4",
    "selenium",
    "apscheduler",
    "tzdata",
]
